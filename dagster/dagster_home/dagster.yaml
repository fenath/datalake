# Localização: ./dagster/dagster_home/dagster.yaml

storage:
  postgres:
    postgres_db:
      username: iceberg
      password: iceberg
      hostname: db
      db_name: dagster_metadata
      port: 5432

retention: # <--- run_retention agora vive aqui dentro
  schedule:
    purge_after_days: 7
  sensor:
    purge_after_days: 7

compute_logs:
  module: dagster.core.storage.local_compute_log_manager
  class: LocalComputeLogManager # <--- Faltava essa linha
  config:
    base_dir: /opt/dagster/dagster_home/storage/compute_logs
